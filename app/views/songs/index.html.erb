<ul>
<% @songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %></li>
  <li>
    <% if display_artist.present? %>
      <%= link_to display_artist, artist_path(song.artist) %>
    <% else %>

    <% end %>
  </li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
